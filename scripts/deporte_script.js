// Función para guardar la selección del tipo de deporte
function guardarDeporteSeleccionado(deporte) {
    // Guardar el deporte seleccionado en localStorage
    localStorage.setItem('deporteSeleccionado', deporte);
}